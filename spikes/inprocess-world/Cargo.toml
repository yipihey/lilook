[package]
name = "lilook-spike-inprocess"
version = "0.1.0"
edition = "2024"

[dependencies]
comemo = "0.5"
ecow = "0.2"
typst = "0.15"
typst-eval = "0.15"
typst-kit = { version = "0.15", features = ["embedded-fonts", "system-files", "system-packages", "universe-packages", "system-downloader", "datetime"] }
typst-layout = "0.15"
typst-render = "0.15"
typst-syntax = "0.15"

[workspace]
