[package]
name = "lilook-ffi"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
lilook-core = { path = "../lilook-core" }
serde_json = { workspace = true }
